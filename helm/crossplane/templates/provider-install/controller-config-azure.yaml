{{ if .Capabilities.APIVersions.Has "pkg.crossplane.io/v1alpha1" }}
{{ if or (eq .Values.giantswarm.kubernetes.provider.kind "azure") (eq .Values.giantswarm.kubernetes.provider.kind "capz") }}
apiVersion: pkg.crossplane.io/v1alpha1
kind: ControllerConfig
metadata:
  name: "{{- include "provider.upboundAzure.name" . }}"
  namespace: {{ .Release.Namespace | quote }}
spec:
  {{- with .Values.giantswarm.crossplane.providers.upboundAzure.controllerConfig.args }}
  args:
    {{- . | toYaml | nindent 4 }}
  {{- end }}
  image: "{{ .Values.giantswarm.images.registry }}/{{ .Values.giantswarm.crossplane.providers.upboundAzure.controllerConfig.image }}:{{ .Values.giantswarm.crossplane.providers.upboundAzure.version }}"
{{ end }}
{{ end }}
